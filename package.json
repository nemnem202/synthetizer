{
  "name": "synthetizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch:wasm": "cd src/sound/rust-synth && cargo watch -w src -s \"wasm-pack build --target web --out-dir build --release\"",
    "watch:worklet": "tsc -p audioworklet.tsconfig.json --watch",
    "dev": "concurrently \"npm run watch:wasm\" \"npm run watch:worklet\" \"vite\"",
    "build": "cd src/sound/rust-synth && wasm-pack build --target web --out-dir build --release && tsc && npx vite build",
    "build:tsonly": "npx vite build",
    "preview": "vite preview --host --port 4173"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.1.6"
  },
  "dependencies": {
    "@types/audioworklet": "^0.0.84",
    "concurrently": "^9.2.1"
  }
}
